<script>
(function(){
  if (!window.DB) { console.warn('[kiosk.legacy] DB no listo'); return; }
  // Ejemplo: si ya tienes lógica moderna, aquí mantén lo básico:
  // - leer catálogo (opcional)
  // - crear order con DB.createOrder(payload)
  // - escuchar HH con DB.subscribeHappyHour(...)
  // Mantengo vacío porque tu kiosko real tiene UI propia ESM.
})();
</script>
